<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.BuscarProdutosActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:id="@+id/buscar_produtos"
        android:background="@color/colorPrimaryDark"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true">

        <EditText
            android:layout_width="230dp"
            android:layout_height="wrap_content"
            android:id="@+id/buscar_produto_nome"
            android:hint="Produto Nome"
            android:textSize="18dp"
            android:textStyle="bold"
            android:background="@android:color/white"
            android:padding="9dp"
            android:layout_marginStart="5dp"
            android:layout_centerHorizontal="true"
            android:textColorHint="@android:color/black"
            />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/buscar_btn"
            android:text="Buscar"
            android:textColor="@android:color/white"
            android:textSize="16dp"
            android:textStyle="bold"
            android:padding="9dp"
            android:layout_centerInParent="true"
            android:background="@color/colorPrimaryDark"
            android:layout_toRightOf="@+id/buscar_produto_nome"
            />

    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/buscar_lista"
        android:layout_below="@+id/buscar_produtos">

    </androidx.recyclerview.widget.RecyclerView>

</RelativeLayout>